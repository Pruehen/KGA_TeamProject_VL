using UnityEngine;
[CreateAssetMenu(fileName = "VFXData", menuName = "VFX/VFXObjcet", order = 0)]
public class VFXObject : ScriptableObject
{
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
